<!-- git-workflow.md: migrated from git-workflow.md.

Hall: hall_rubric
-->

Branch from `main` for each feature. Commit small, logically atomic units — one fix, one refactor, one test addition. Never force-push to shared branches. Merge features back to `main` via fast-forward ref update, not rebase-and-push workflows. No `git push --force` to shared branches.
