## Implementation status

v0.1 includes this template as content guidance. The `bees ship` code that populates these placeholders dynamically is **Tier-2 BACKLOG**. Currently, `bees ship` generates a minimal body; this template documents what the eventual implementation should target.
