## Tone and depth

The PR body should be reviewable without the operator reading the full plan. Linked issues auto-close on merge, so the syntax (`Closes #N`) is preserved. The summary should fit in 2–3 sentences; if more is needed, the reviewer can click through to the plan.
