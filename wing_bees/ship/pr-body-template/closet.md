<!-- pr-body-template.md: migrated from pr-body-template.md.

Hall: hall_protocol
-->

PR body opened by `bees ship` has five sections: **Summary** (plan Context first paragraph), **Work** (spec links with NNN slugs), **Test plan** (per-spec failing-test verification), **Linked issues** (auto-close syntax preserved), **Risks** (plan risk section); footer carries bees version, Plan link, and Honeycomb-trace link. Template is content-only; dynamic `{{…}}` placeholder population tracked in BACKLOG.
