## Scoring

The keyword scorer matches query terms against room names, closet text, and body content; ties are broken by wing then room name (alphabetical). Cheap, deterministic, no embedding dependency — and always available.
