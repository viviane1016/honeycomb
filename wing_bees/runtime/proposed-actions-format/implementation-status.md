## Implementation status

The format is defined here. In v0.1, the proposed-actions list is parsed and presented to the operator, but automation of approval-flow and execution is deferred. The foundation (format + per-action approval UI/CLI) ships in post-v0.1 BACKLOG.
