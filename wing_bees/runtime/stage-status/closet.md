<!-- stage-status.md: migrated from stage-status.md.

Hall: hall_procedure
tools: [gh]
-->

The status stage reports feature progress without changing anything. It surfaces plan/spec/cell state, the open PR (via `gh`), CI status, and ahead-count over the base branch. This is useful after ship — check status to see whether CI has finished and whether the PR is ready to merge.
