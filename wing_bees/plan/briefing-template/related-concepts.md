## Related concepts

- Classifier tags (in `wing_bees/classifier-prompt`): what tags the queen uses internally
- Plan stage (in `wing_bees/stage-plan`): how the queen runs after reading your briefing
- PR body template (in `wing_bees/pr-body-template`): where briefing context flows on merge
- Accept stage (in `wing_bees/stage-accept`): how the queen reads operator amendments to verify intent at ship time
