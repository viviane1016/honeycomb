## Expected outcome
Users can log in via GitHub or Google OAuth. Existing username/password auth remains available. On first OAuth login, we auto-provision an account; subsequent logins merge with the existing account if email matches. Sessions work across auth methods.
