## Bypasses

Set `BEES_ACCEPT_DISABLE=1` (environment variable) or pass `bees ship --skip-accept` (flag) to skip queen-accept for emergencies. Both are logged in `events.log` with `outcome="disabled"`.
