## Numbering

To find the next free NNNN, glob `decisions/` for `[0-9][0-9][0-9][0-9]-*.md` and take the highest prefix + 1. The `stage-design` honeycomb room automates this when producing ADR proposals.
