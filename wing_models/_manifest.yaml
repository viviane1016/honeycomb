scope: runtime

mandatory_drawers:
  - closet.md
  - index.md
  - Readme.md

# Runtime-scoped: model selection rubric and per-model prompt adaptations.
# Sits alongside wing_bees (and future tool wings) because prompt
# adaptations are runtime apparatus, not catalog content.
canonical_rooms:
  - selection       # which model for which job
  - adaptation      # per-model prompt forms (Haiku vs Sonnet vs Opus phrasing)
  - benchmarks
  - cost
