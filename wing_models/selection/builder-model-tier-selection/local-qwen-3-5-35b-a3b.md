## local:qwen-3.5-35b-a3b

### Closet

Petition required to recommend for any tier. v0.1 routed sonnet/haiku auto-substituted to Qwen; v0.2 removes that substitution. The queen now tags `local:*` only when explicitly justified. See #67 for routing meta-tracking, substitution-strip closing the data-collection-shim era.

### Drawer

Qwen failure modes documented during the v0.1 dispatch experiment: tool-call schema drift (#48), premature commit-without-tests (#56), and silent loop exits (#64). Until the petition workflow lands in Hive 0.1, the queen should not tag specs to Qwen unless a project-level `.bees/queen.md` explicitly authorises it.
